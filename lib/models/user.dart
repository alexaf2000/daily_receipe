/// `User` model used for identify the logged
/// user.
class User {
  String name;
  String surname;
  String email;

  // Constructor
  User({
    this.name,
    this.surname,
    this.email,
  });
}
